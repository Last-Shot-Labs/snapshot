import type { CardConfig } from "./types";
/**
 * Manifest adapter — resolves config refs, renders manifest children,
 * delegates layout to CardBase.
 */
export declare function Card({ config }: {
    config: CardConfig;
}): import("react/jsx-runtime").JSX.Element;
