// SSE hooks are now created directly inside the createSnapshot closure
// via the per-endpoint registry pattern. This file is intentionally empty.
// See create-snapshot.tsx for useSSE() and useSseEvent() implementations.
